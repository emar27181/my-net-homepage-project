---
import SimpleLayout from "../layouts/SimpleLayout.astro";
import YouTubeClipViewer from "../components/YouTubeClipViewer.astro";
import { applyTypographyPreset } from "../utils/typography";
import "../styles/global.css";

// フォントサイズ設定
const FONT_SIZES = {
  title: "24px", // メインタイトル
  subtitle: "16px", // サブタイトル
  sectionTitle: "18px", // セクション見出し
  subTitle: "16px", // 小見出し
  bodyText: "14px", // 本文テキスト
  smallText: "10px", // 小さいテキスト
  footer: "10px", // フッター
};

// 固定カラー設定
const COLORS = {
  // メインカラー
  primary: "#4d79ff", // ネオンブルー
  primaryAlpha10: "rgba(77,121,255,0.1)", // 10%透明度
  primaryAlpha5: "rgba(77,121,255,0.05)", // 5%透明度
  primaryShadow: "rgba(77,121,255,0.3)", // シャドウ用

  // タイトル・見出しカラー
  heading: "#A6F7FF", // ライトシアン（タイトル・見出し専用）

  // テキストカラー
  textWhite: "#ffffff", // 白文字
  textGray: "#cccccc", // グレー文字
  textDarkGray: "#666666", // ダークグレー文字
  textBlack: "#000000", // 黒文字（シャドウ用）

  // 背景・ボーダー
  bgWhite: "#ffffff", // 白背景
  bgTransparent: "transparent", // 透明

  // エフェクト
  shadowWhite: "rgba(255,255,255,0.3)", // 白シャドウ
  shadowBlack: "rgba(0,0,0,0.5)", // 黒シャドウ
};
---

<SimpleLayout title="nodoame's Portfolio">
  <div
    style={`max-width: 1000px; margin: 0 auto; padding: 20px; background: var(--color-bg-secondary); border: 4px solid ${COLORS.primary}; border-radius: 12px; box-shadow: inset 2px 2px 0px ${COLORS.shadowWhite}, inset -2px -2px 0px ${COLORS.shadowBlack}, 0 0 20px ${COLORS.primaryShadow}; min-height: 100vh;`}
    class="main-container mobile-main"
  >
    <!-- タイトル -->
    <div style="text-align: center; margin-bottom: 40px;" class="title-section">
      <h1
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.title}; margin-bottom: 15px; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
        class="main-title"
      >
        <span class="title-first-line">Nodoame's</span> <span class="title-second-line">Portfolio</span>
      </h1>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 15px auto; width: 100%;`}
      />

      <!-- プロフィール画像 -->
      <img
        src="/icon_omencat_transparent.png"
        alt="nodoame profile"
        style={`width: 120px; height: 120px; margin: 0 auto 15px auto; display: block; border-radius: 8px; border: 2px solid ${COLORS.primary}; background-color: ${COLORS.primaryAlpha10}; padding: 5px;`}
      />

      <p
        style={`color: ${COLORS.textWhite}; font-size: ${FONT_SIZES.subtitle}; margin: 0;`}
        class={applyTypographyPreset("mainText")}
      >
        @nodoame27181 のポートフォリオ
      </p>
    </div>

    <!-- About Me -->
    <div
      style={`background: ${COLORS.primaryAlpha10}; border: 2px solid ${COLORS.primary}; padding: 20px; margin-bottom: 40px; border-radius: 8px;`}
      class="about-section"
    >
      <h2
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.sectionTitle}; margin-bottom: 15px; text-align: center; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
      >
        ◆ About Me ◆
      </h2>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 20px auto; width: 100%;`}
      />

      <div style="text-align: center; margin-bottom: 15px;">
        <p
          style={`color: ${COLORS.textGray}; font-size: ${FONT_SIZES.bodyText}; line-height: 1.8; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
        >
          VALORANTをメインにプレイしている猫好きなゲーマーです。<br />
          <br />
          ゲーム以外にもWeb制作やプログラミングに興味があり、<br />
          このポートフォリオサイトを制作しました。<br />
          最近はVALORANTの戦略研究やマップ分析にも力を入れています。<br />
          <br />
          ※このサイトはClaude Codeを用いて作られました。
        </p>
      </div>
    </div>

    <!-- プロフィール情報 -->
    <div
      style={`background: ${COLORS.primaryAlpha10}; border: 2px solid ${COLORS.primary}; padding: 20px; margin-bottom: 40px; border-radius: 8px;`}
      class="profile-section"
    >
      <h2
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.sectionTitle}; margin-bottom: 15px; text-align: center; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
      >
        ★ Player Info ★
      </h2>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 20px auto; width: 100%;`}
      />

      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;" class="profile-info-grid">
        <div>
          <h3
            style={`color: ${COLORS.textWhite}; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            基本情報
          </h3>
          <p
            style={`color: ${COLORS.textGray}; font-size: ${FONT_SIZES.bodyText}; line-height: 1.8; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            名前: nodoame<br />
            メインロール: コントローラー(オーメン/クローブ/アストラ) <br />
          </p>
        </div>

        <div>
          <h3
            style={`color: ${COLORS.textWhite}; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            外部リンク
          </h3>
          <p
            style={`color: ${COLORS.textGray}; font-size: ${FONT_SIZES.bodyText}; line-height: 1.8; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            <svg style="display: inline-block; width: 16px; height: 16px; vertical-align: middle; margin-right: 5px;" viewBox="0 0 24 24" fill="currentColor">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
            </svg>Twitter: <a
              href="https://twitter.com/nodoame27181"
              target="_blank"
              rel="noopener"
              style={`color: ${COLORS.primary};`}>@nodoame27181</a
            ><br />
            <svg style="display: inline-block; width: 16px; height: 16px; vertical-align: middle; margin-right: 5px;" viewBox="0 0 24 24" fill="currentColor">
              <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
            </svg>Youtube: <a
              href="https://www.youtube.com/@nodoame27181"
              target="_blank"
              rel="noopener"
              style={`color: ${COLORS.primary};`}>@nodoame27181</a
            ><br />
            <svg style="display: inline-block; width: 16px; height: 16px; vertical-align: middle; margin-right: 5px;" viewBox="0 0 24 24" fill="currentColor">
              <path d="M3 3v18h18V3H3zm16 16H5V5h14v14zM7 10h2v7H7v-7zm4-3h2v10h-2V7zm4 6h2v4h-2v-4z"/>
            </svg>TRN: <a
              href="https://tracker.gg/valorant/profile/riot/nodoame%2327181/overview?platform=pc&playlist=competitive&season=4c4b8cff-43eb-13d3-8f14-96b783c90cd2"
              target="_blank"
              rel="noopener"
              style={`color: ${COLORS.primary};`}>Tracker Profile</a
            ><br />
          </p>
        </div>
      </div>
    </div>

    <!-- Works -->
    <div
      style={`background: ${COLORS.primaryAlpha10}; border: 2px solid ${COLORS.primary}; padding: 20px; margin-bottom: 40px; border-radius: 8px;`}
      class="works-section"
    >
      <h2
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.sectionTitle}; margin-bottom: 15px; text-align: center; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
      >
        ◆ My Works ◆
      </h2>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 20px auto; width: 100%;`}
      />

      <div style="display: grid; grid-template-columns: 1fr; gap: 15px;">
        <!-- VALORANT Point Viewer -->
        <div
          style={`background: ${COLORS.primaryAlpha5}; border: 1px solid ${COLORS.primary}; padding: 15px; border-radius: 6px;`}
        >
          <!-- デスクトップ: 左右レイアウト -->
          <div
            style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; align-items: center;"
            class="desktop-layout"
          >
            <!-- 左側: 説明とリンク -->
            <div>
              <h3
                style={`color: #4d79ff; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px #000000;`}
              >
                VALORANT Point Viewer (現在作成途中)
              </h3>
              <p
                style={`color: #cccccc; font-size: ${FONT_SIZES.bodyText}; line-height: 1.6; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px #000000; margin-bottom: 15px;`}
              >
                YouTubeショートやリールで見つけた定点動画を試合中に再現したいのに、別マップや別エリアの動画が混ざっていて探しにくい…。それを解消するために、マップ上に定点動画のリンクを直接プロットし、必要な場所の動画だけを素早く確認できるようにしたサイトです。<br
                />
              </p>

              <div style="text-align: left;">
                <a
                  href="https://valorant-point-viewer.netlify.app/"
                  target="_blank"
                  rel="noopener"
                  style={`display: inline-block; padding: 6px 12px; background: ${COLORS.primary}; color: ${COLORS.textWhite}; text-decoration: none; border-radius: 4px; font-size: ${FONT_SIZES.smallText}; font-family: 'Press Start 2P', monospace; text-shadow: none; transition: all 0.3s ease;`}
                  onmouseover={`this.style.background='${COLORS.bgTransparent}'; this.style.color='${COLORS.primary}'; this.style.border='1px solid ${COLORS.primary}';`}
                  onmouseout={`this.style.background='${COLORS.primary}'; this.style.color='${COLORS.textWhite}'; this.style.border='none';`}
                >
                  サイトを見る →
                </a>
              </div>
            </div>

            <!-- 右側: 実際のサイト埋め込み -->
            <div style="text-align: center;">
              <div
                style="position: relative; width: 100%; aspect-ratio: 2 / 3;"
              >
                <iframe
                  src="https://valorant-point-viewer.netlify.app/"
                  width="100%"
                  height="100%"
                  frameborder="0"
                  scrolling="no"
                  style={`border: 2px solid ${COLORS.primary}; border-radius: 6px; box-shadow: 0 4px 8px ${COLORS.primaryShadow}; background: ${COLORS.bgWhite}; position: absolute; top: 0; left: 0; overflow: hidden;`}
                  title="VALORANT Point Viewer - Live Demo"
                  loading="lazy"></iframe>
              </div>
            </div>
          </div>

          <!-- モバイル: 縦レイアウト -->
          <div style="display: none;" class="mobile-layout">
            <h3
              style={`color: #4d79ff; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px #000000;`}
            >
              🎯 VALORANT Point Viewer
            </h3>
            <p
              style={`color: #cccccc; font-size: ${FONT_SIZES.bodyText}; line-height: 1.6; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px #000000; margin-bottom: 15px;`}
            >
              VALORANTの各マップの定点動画をマップ上で確認できるWebサイトです。<br
              />
              プレイヤーがマップを理解し、戦略を練るためのツールとして制作しました。
            </p>

            <div style="margin-bottom: 15px; text-align: center;">
              <div
                style="position: relative; width: 100%; max-width: 350px; height: calc((350px * 4 / 3) + 20px); margin: 0 auto;"
              >
                <iframe
                  src="https://valorant-point-viewer.netlify.app/"
                  width="100%"
                  height="100%"
                  frameborder="0"
                  scrolling="no"
                  style={`border: 2px solid ${COLORS.primary}; border-radius: 6px; box-shadow: 0 4px 8px ${COLORS.primaryShadow}; background: ${COLORS.bgWhite}; position: absolute; top: 0; left: 0; overflow: hidden;`}
                  title="VALORANT Point Viewer - Live Demo (Mobile)"
                  loading="lazy"></iframe>
              </div>
            </div>

            <div style="text-align: center;">
              <a
                href="https://valorant-point-viewer.netlify.app/"
                target="_blank"
                rel="noopener"
                style={`display: inline-block; padding: 6px 12px; background: ${COLORS.primary}; color: ${COLORS.textWhite}; text-decoration: none; border-radius: 4px; font-size: ${FONT_SIZES.smallText}; font-family: 'Press Start 2P', monospace; text-shadow: none; transition: all 0.3s ease;`}
                onmouseover="this.style.background='transparent'; this.style.color='#4d79ff'; this.style.border='1px solid #4d79ff';"
                onmouseout="this.style.background='#4d79ff'; this.style.color='#ffffff'; this.style.border='none';"
              >
                サイトを見る →
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- YouTube動画セクション -->
    <div
      style={`background: ${COLORS.primaryAlpha10}; border: 2px solid ${COLORS.primary}; padding: 20px; margin-bottom: 40px; border-radius: 8px;`}
      class="video-section"
    >
      <h2
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.sectionTitle}; margin-bottom: 15px; text-align: center; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
      >
        ★ VALORANT Gameplay ★
      </h2>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 20px auto; width: 100%;`}
      />

      <div class="video-grid">
        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/ERb40ovB060"
            title="VALORANT Clip #1"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/KTwFA1jQgFY"
            title="VALORANT Clip #2"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/oos2PYWiRGM"
            title="VALORANT Clip #3"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://www.youtube.com/watch?v=5IET-4mL9pA"
            title="VALORANT Clip #4"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/46a0f7dNVW4?si=FjCr_YPa35-guCZX"
            title="VALORANT Clip #5"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/Yznxiz_tS4g?si=sWJFRV_vB95mkJNl"
            title="VALORANT Clip #6"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/ooJdHWZ3KyU?si=pS2rqHsxPnuRqmEV"
            title="VALORANT Clip #7"
            autoplay={false}
          />
        </div>

        <div>
          <YouTubeClipViewer
            clipUrl="https://youtu.be/Aazj-kAuxdY"
            title="VALORANT Clip #8"
            autoplay={false}
          />
        </div>
      </div>
    </div>

    <!-- デバイス情報 -->
    <div
      style={`background: ${COLORS.primaryAlpha10}; border: 2px solid ${COLORS.primary}; padding: 20px; margin-bottom: 40px; border-radius: 8px;`}
      class="device-section"
    >
      <h2
        style={`color: ${COLORS.heading}; font-size: ${FONT_SIZES.sectionTitle}; margin-bottom: 15px; text-align: center; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack}; letter-spacing: 1px;`}
      >
        ◎ Setup & Settings ◎
      </h2>

      <!-- 横線 -->
      <hr
        style={`border: none; height: 1px; background-color: ${COLORS.primary}; margin: 0 auto 20px auto; width: 100%;`}
      />

      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;" class="device-settings-grid">
        <div>
          <h3
            style={`color: ${COLORS.textWhite}; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            デバイス
          </h3>
          <p
            style={`color: ${COLORS.textGray}; font-size: ${FONT_SIZES.bodyText}; line-height: 1.8; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            マウス: Logicool G Pro X<br />
            キーボード: Akko 5075S HE JP Dark Night<br />
            ヘッドセット: SteelSeries Arctis 7<br />
            モニター: 144Hz 24インチ
          </p>
        </div>

        <div>
          <h3
            style={`color: ${COLORS.textWhite}; font-size: ${FONT_SIZES.subTitle}; margin-bottom: 10px; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            ゲーム設定
          </h3>
          <p
            style={`color: ${COLORS.textGray}; font-size: ${FONT_SIZES.bodyText}; line-height: 1.8; font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
          >
            感度: 0.09 (1600 DPI)<br />
            クロスヘア: 青色・アウトライン有<br />
            クロスヘアコード:
            <span id="copy-crosshair-container" style="display: inline-block;">
              <svg
                id="copy-crosshair-icon"
                style="display: inline-block; width: 14px; height: 14px; vertical-align: middle; margin-right: 5px; cursor: pointer; fill: #4d79ff;"
                title="クリックでコピー"
                viewBox="0 0 24 24"
              >
                <path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/>
              </svg>
              <svg
                id="check-crosshair-icon"
                style="display: none; width: 14px; height: 14px; vertical-align: middle; margin-right: 5px; fill: #4d79ff;"
                viewBox="0 0 24 24"
              >
                <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
              </svg>
            </span><span class="crosshair-code">0;p;0;s;1;P;c;8;u;0077FFFF;o;0.3;b;1;f;0;0t;1;0l;5;0v;5;0o;2;0a;1;0f;0;1t;3;1o;2;1a;1;1m;0;1f;0;A;c;5;u;0077FFFF;o;0.2;0t;3;0l;1;0v;1;0o;1;0a;1;0f;0;1b;0;S;c;5</span><br />
            解像度: 1920×1080<br />
            FPS: 144fps
          </p>
        </div>
      </div>
    </div>

    <!-- フッター -->
    <div
      style="text-align: center; padding: 20px 0; border-top: 1px solid #333333;"
    >
      <p
        style={`color: ${COLORS.textDarkGray}; font-size: ${FONT_SIZES.footer}; margin: 0; font-family: 'Press Start 2P', monospace; image-rendering: pixelated; text-shadow: 1px 1px 0px ${COLORS.textBlack};`}
      >
        © 2024 nodoame's Portfolio - Made with passion for gaming
      </p>
    </div>
  </div>
</SimpleLayout>

<style>
  /* 文字溢れ防止 - ページ全体の自動改行ルール */
  * {
    word-wrap: break-word !important;
    word-break: break-all !important;
    overflow-wrap: break-word !important;
  }

  /* 長い文字列（クロスヘアコードなど）の特別対応 */
  p, span, div {
    hyphens: auto;
    -webkit-hyphens: auto;
    -moz-hyphens: auto;
    -ms-hyphens: auto;
  }

  /* クロスヘアコード専用スタイル */
  .crosshair-code {
    font-family: 'DotGothic16', 'BIZ UDPGothic', 'Press Start 2P', monospace !important;
    background-color: transparent !important;
    padding: 0 !important;
    border-radius: 0 !important;
    word-break: break-all !important;
    overflow-wrap: break-word !important;
    display: inline-block !important;
    image-rendering: pixelated !important;
    text-shadow: 1px 1px 0px #000000 !important;
  }

  /* スマートフォン向け追加改行ルール */
  @media screen and (max-width: 768px) {
    * {
      word-break: break-all !important;
      overflow-wrap: anywhere !important;
      line-break: anywhere !important;
    }
    
    /* 長いコード文字列用の強制改行 */
    span, code {
      white-space: pre-wrap !important;
      word-break: break-all !important;
    }

    /* クロスヘアコードのモバイル対応 */
    .crosshair-code {
      display: block !important;
      margin: 5px 0 !important;
      white-space: pre-wrap !important;
      word-break: break-all !important;
      overflow-wrap: anywhere !important;
      max-width: 100% !important;
    }

    /* グリッドレイアウトを1列に変更 */
    .profile-info-grid,
    .device-settings-grid {
      grid-template-columns: 1fr !important;
      gap: 15px !important;
    }

    /* タイトルの改行対応 */
    .title-second-line {
      display: block !important;
      margin-top: 10px !important;
    }

    /* スマートフォン向け余白調整 */
    .main-container {
      padding: 10px !important;
      margin: 0px !important;
      border: none !important;
      border-radius: 0px !important;
      box-shadow: none !important;
      max-width: 100vw !important;
      min-height: 100vh !important;
    }

    /* 各セクションの余白を減少 */
    .title-section {
      margin-bottom: 15px !important;
      padding: 5px !important;
    }

    .about-section,
    .profile-section,
    .works-section,
    .video-section,
    .device-section {
      padding: 8px !important;
      margin-bottom: 15px !important;
    }

    /* グリッド内の個別要素の余白調整 */
    .profile-info-grid > div,
    .device-settings-grid > div {
      padding: 0 3px !important;
      margin: 0 !important;
    }

    /* テキスト要素の余白調整 */
    .about-section p,
    .profile-section p,
    .device-section p {
      padding: 0 5px !important;
      margin: 10px 0 !important;
    }

    /* Works セクション内の特別調整 */
    .works-section > div > div {
      padding: 10px !important;
    }
  }

  /* 動画グリッドのレスポンシブ対応 */
  .video-grid {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 20px !important;
    width: 100% !important;
  }

  /* デスクトップ表示（769px以上） */
  @media screen and (min-width: 769px) {
    .video-grid {
      grid-template-columns: repeat(2, 1fr) !important;
      gap: 20px !important;
    }
  }

  /* タブレット表示（481px-768px） */
  @media screen and (min-width: 481px) and (max-width: 768px) {
    .video-grid {
      grid-template-columns: repeat(2, 1fr) !important;
      gap: 15px !important;
    }
  }

  /* スマホ表示 */
  @media screen and (max-width: 480px) {
    .video-grid {
      grid-template-columns: 1fr !important;
      gap: 20px !important;
    }
  }

  /* My Works レスポンシブ対応 */
  @media screen and (max-width: 768px) {
    .desktop-layout {
      display: none !important;
    }

    .mobile-layout {
      display: block !important;
    }
  }

  @media screen and (min-width: 769px) {
    .desktop-layout {
      display: grid !important;
    }

    .mobile-layout {
      display: none !important;
    }
  }
</style>

<script>
  function copyCrosshairCode() {
    navigator.clipboard.writeText('0;p;0;s;1;P;c;8;u;0077FFFF;o;0.3;b;1;f;0;0t;1;0l;5;0v;5;0o;2;0a;1;0f;0;1t;3;1o;2;1a;1;1m;0;1f;0;A;c;5;u;0077FFFF;o;0.2;0t;3;0l;1;0v;1;0o;1;0a;1;0f;0;1b;0;S;c;5');
    
    // アイコンを切り替え
    const copyIcon = document.getElementById('copy-crosshair-icon');
    const checkIcon = document.getElementById('check-crosshair-icon');
    
    if (copyIcon && checkIcon) {
      copyIcon.style.display = 'none';
      checkIcon.style.display = 'inline-block';
      
      // 2秒後に元に戻す
      setTimeout(() => {
        checkIcon.style.display = 'none';
        copyIcon.style.display = 'inline-block';
      }, 2000);
    }
  }
  
  // DOM読み込み完了後に実行
  document.addEventListener('DOMContentLoaded', function() {
    const copyIcon = document.getElementById('copy-crosshair-icon');
    if (copyIcon) {
      copyIcon.addEventListener('click', copyCrosshairCode);
    }
  });
</script>
